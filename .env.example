# OpenAI (required)
OPENAI_API_KEY=your-openai-key-here
OPENAI_MODEL=gpt-4.1
OPENAI_CODEX_MODEL=o3

# GitHub (optional; only needed if working with GitHub-hosted repos or private repos)
GITHUB_TOKEN=your-github-token-here

# Multiple ADTs (optional for reviewer mode; not needed for creator mode)
# Provide a space-separated list of repository URLs (SSH or HTTPS). Example:
# ADTS="git@github.com:unicef/ADT-manual-autocuidados.git git@github.com:unicef/ADT-cuaderno5-chapter1.git"
ADTS=

# ADT Utils repo (required)
# Default value points to the official UNICEF utils repo
ADT_UTILS_REPO=git@github.com:unicef/adt-utils.git

# Optional: Increase wait time (seconds) for the API to become ready on slower systems
# STARTUP_TIMEOUT=180
